class_path: asymdsd.data.StampaAsymDSDDataModule
init_args:
  root_dir: /home/ubuntu/dataset/parts_no_trace
  version: 
  train_split: train
  val_split: validation
  n_points: 8192
  normalize: src
  data_augmentation: true
  verbose: false
  num_cache_workers: 16
  cache_dir:
  eval_split_ratio: 0.1
  split_seed: 0
  batch_size: 64
  num_workers_train: 8
  num_workers_val_test: 0
  pin_memory: true
